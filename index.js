function greet(name, lastName) { // function declaration (here to declare parameters)
    console.log('Hello ' + name + ' ' + lastName);
}

greet('Janos', 'Stelli'); // function calling (here to pass arguments)

// parameter at time of delcaration
// argument is the actual value of supply for that paramter 

