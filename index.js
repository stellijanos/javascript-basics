
let name = 'Janos';
console.log(name); // by default all variables are 'undefined'

// cannot be a reserved keyword
// should be meaningful
// cannot start with a number (1name - not ok)
// cannot contain a space or hyphen (-) 
// are case sensitive

let firstName = 'Janos';  // camel notation (case)
let lastname = 'Stelli';
